---
name: Shell
id: modules.shell
flake: xinux-modules
unique_groups:
  - shell
description: "Shell with its configurations."
version: 0.0.2
icon:
  type: system
  path: com.mitchellh.ghostty-symbolic

options:
  - label: "Shell Type"
    id: modules.shell.type
    description: "Choose shell to your liking."
    type: !enum
      default: zsh
      options:
        zsh: Zsh
        bash: Bash
  - label: "Direnv"
    id: modules.shell.direnv
    description: "Enable direnv integration for the shell."
    type: !switch
      default: false
  - label: "Rusted Tool"
    id: modules.shell.rusted-tools
    description: "Replace core-utils with rust rewrite of improved versions."
    type: !switch
      default: false
  - label: "Colors"
    id: modules.shell.colors
    description: "Add more colors to shell (P.S too much colors)."
    type: !switch
      default: false
  - label: "Starship"
    id: modules.shell.starship
    description: "Add custom shell prompt via starship."
    type: !switch
      default: false
